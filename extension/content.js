// content.js — AntCapture
// This script is injected into the Web UI pages by the extension.
// It acts as a bridge: the Web UI can dispatch custom events here
// and this script forwards them to the extension's background service worker.

// Currently no actions needed from the Web UI side — recording is handled
// directly from the extension popup. This file is kept for future use.
