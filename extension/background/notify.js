// background/notify.js — AntCapture
// Notifications have been entirely disabled by user request.
// This is now a no-op function to prevent breaking existing imports.

export function notify(id, title, message) {
  // Notifications disabled.
}
