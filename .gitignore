node_modules/
.env
skills/

# Built extension packages — never commit these
*.zip

# Marketing screenshots — local only, never commit
marketing/

# Local build planning & architecture notes — never commit
_build-info/
_manual-tests/
