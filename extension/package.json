{
  "name": "extension",
  "version": "1.0.0",
  "description": "The extension works **completely standalone** — no build step, no npm, no codebase required.",
  "main": "background.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "eslint ."
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.6.0",
    "globals": "^17.7.0"
  },
  "type": "module",
  "dependencies": {
    "uploadthing": "^7.7.4"
  }
}
